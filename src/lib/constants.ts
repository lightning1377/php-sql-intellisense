export const tableNameKeywords = ["FROM", "JOIN", "INTO", "UPDATE"];
export const fieldNameKeywords = ["SELECT", "SET", "ON", "BY"];
export const allKeyWords = [...tableNameKeywords, ...fieldNameKeywords];
